<resources>
    <style name="Theme.HealthApp" parent="Theme.Material3.DayNight">
        <!-- Transparent system bars to blend Compose content with the system UI -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Allow content to extend behind system bars on modern devices -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
